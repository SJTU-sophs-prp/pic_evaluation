import torch
import torch.nn.functional as F
import numpy as np
import math
from PIL import Image
import cv2
import os

arr = []

add = 1

arr.append(add)
arr.append(2)
arr.append(22)
arr.append(3)

print(arr)
print(np.median(arr))
print(np.mean(arr))
